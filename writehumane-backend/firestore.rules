rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No direct client access — all access via Cloud Functions only
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
